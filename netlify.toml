[build]
  # Builds are handled by GitHub Actions — skip Netlify's own auto-deploy.
  ignore = "exit 0"

# SPA fallback — all routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
