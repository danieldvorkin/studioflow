Reset your StudioFlow password

Hi <%= @user.name.presence || @user.email %>,

We received a request to reset the password for your StudioFlow account.

Reset your password here: <%= @reset_url %>

This link expires in 2 hours. If you didn't request a password reset, you can safely ignore this email — your password won't change.

— The StudioFlow Team
